# Modern UI 26.2 移植 — 项目管理与决策记录

> 记录本仓库的结构演进、关键技术决策与已修复问题，供后续维护参考。
> 项目主页：https://github.com/Huziyang520/ModernUI-Unofficial-Port

---

## 1. 项目概况

| 项目 | 内容 |
|---|---|
| 目标 | 将 **Modern UI** 移植到 **Minecraft 26.2**，同时支持 NeoForge 与 Fabric |
| 加载器 | NeoForge `26.2.0.88`、Fabric Loader `0.19.3+` |
| Java | 25 |
| 模组版本 | `3.13.0.8-SNAPSHOT`（移植期间保持不变） |
| 核心库 | `dev.icyllis:modernui-core:3.13.0`（未修改，Maven 引入） |
| 协议 | LGPL-3.0-or-later |
| 已放弃 | Forge（`forge/` 模块已移除） |

### 来源与归属

- **框架原作者**：[BloCamLimb](https://github.com/BloCamLimb/ModernUI)，本项目的全部基础框架来自该仓库。
- **Fabric 26.2 移植**：[DragonHua](https://github.com/drangonmc/ModernUI-Fabric26.2) 完成的成果，本项目 Fabric 端即基于其工作。
- **本项目**：在其二人基础上完成 NeoForge 26.2 端移植，并将两端合并为统一的多加载器工程。

---

## 2. 结构演进

### 2.1 初始状态（合并前）

| 目录 | 内容 |
|---|---|
| `ModernUI-MC/` | 上游 `BloCamLimb/ModernUI-MC` 的 clone（MultiLoader，但只启用了 common + neoforge），即"NeoForge 26.2 移植版" |
| `ModernUI-Unofficial-Port-master/` | Fabric 26.2 移植版（无 `.git`，为下载快照） |
| `ModernUI/26.2/` | 空 |

两份工程**同源但各有新旧**，需要逐模块裁决。

### 2.2 最终结构

```
ModernUI/26.2/
├─ common/      共享代码、Mixin、着色器、资源
├─ neoforge/    NeoForge 入口与平台适配
├─ fabric/      Fabric 入口与平台适配
├─ libs/        随仓库携带的构建期依赖（ModernUI-Fonts-5.0.jar，必需）
├─ assets/      Blockbench 模型等美术源文件
└─ gradle/      Gradle wrapper
（forge/ 已删除；build/ 与 .gradle/ 为构建缓存，已被 .gitignore 排除）
```

仓库关联：`origin` = `Huziyang520/ModernUI-Unofficial-Port`，`upstream` = `BloCamLimb/ModernUI-MC`。

---

## 3. 合并决策

差异分析结果（源码对比，排除构建产物）：`common` 41 个文件、`fabric` 7 个、`neoforge` 5 个。

| 模块 | 采用版本 | 决策依据 |
|---|---|---|
| `fabric/` | **Unofficial-Port 版** | 26.2 移植版（fabric-api `0.158.0+26.2`、loader `0.19.3`、Mod Menu `20.0.0-beta.2`、Forge Config API Port `26.2.1`、VulkanMod compile-only）；本仓库原 `fabric/` 仍是 26.1.2 配置 |
| `neoforge/` | **26.2 版** | 含 26.2 生效的构建配置（`exclude net/minecraft/**` 防止 JPMS 分包冲突）；对方版本仍在使用已被 26.2 删除的 `tooltip` 注入、依赖区间写的是 `[26.1,26.2)` |
| `common/` | **26.2 版** | 更新（含 textmc 修复） |
| `forge/` | **删除** | 本次目标为双加载器（NeoForge + Fabric），不再维护 Forge |
| `MixinGui.java` | **从原 Fabric 版恢复** | Fabric 的 mixin 配置引用它，而 26.2 版将其删除；不恢复则运行时崩溃 |

### 配置统一

- `settings.gradle`：直接 include `common` + `neoforge` + `fabric`，移除 Forge 与 `-PfabricOnly` 开关。
- `gradle.properties`：`mod_version=3.13.0.8-SNAPSHOT`、`minecraft_version=26.2`、`neoforge_version=26.2.0.88`，删除 `forge_version`。
- `common/build.gradle`：VulkanMod 依赖由 `api` 改为 **`compileOnly`**（可选集成，不打包、不作为运行时依赖）。
- `neoforge/build.gradle`：因 neoforge 会一并编译 common 源码，需自行补充 VulkanMod `compileOnly`（`compileOnly` 不跨模块传递）。

---

## 4. 已修复问题

按发现顺序记录，均与 MC 26.2 的 API/运行时变更有关。

### 4.1 Mixin 目标失效（启动崩溃）

| 问题 | 原因 | 处理 |
|---|---|---|
| `Minecraft` 找不到 `@Shadow field screen` | 26.2 将 `Minecraft.screen` 移至 `Gui`，`setScreen` 改名 `setScreenAndShow` | 改用 `gui.screen()` + `setScreenAndShow` |
| `@Mixin target MultiBufferSource$BufferSource was not found` | 26.2 删除了该类 | 从 textmc 配置移除 `AccessBufferSource` |
| `Minecraft.renderFrame` 注入 `RenderSystem.flipFrame` 失败 | 26.2 改为 `GpuSurface.present()` | 更新注入目标 |

### 4.2 Mixin 包可见性（启动崩溃）

`IllegalClassLoadError: ... is in a defined mixin package ... and cannot be referenced directly`：

- `IChatFormattingAccessor` 被普通类 `MuiModApi` 引用 → **移出 mixin 包**至 `icyllis.modernui.mc`。
- `IGuiTextRenderStateAccessor` 带 `@Mixin` 但未注册 → **补注册**进 textmc 配置。
- 清理 `TextRenderType` 中对已失效 `AccessBufferSource` 的死引用。

### 4.3 文本渲染（文字全部不可见）

26.2 将文本着色器合并为 `minecraft:core/text` 并用宏切换，旧名 `rendertype_text_intensity` 已删除，导致 GUI 文本管线编译失败。修复分三层：

1. **管线配置**：派生自 vanilla 管线（复制 shader defines / bind groups / 顶点格式，仅替换 fragment shader）。
   注意：**编译期使用的 MC jar 与运行时 API 不同**（`minecraft-merged-deobf` 中 `RenderPipeline#toBuilder()` 不存在、vanilla snippet 为 private），故采用手工 `copyOf` 复制。
2. **着色器**：自定义 fsh 升级为 `#version 330` 与 26.2 `text.vsh` 对齐；雾相关代码用 `#if !defined(IS_GUI) && !defined(IS_SEE_THROUGH)` 守卫（26.2 仅在该条件下输出雾 varying）。
3. **Uniform 绑定**：必须继承 vanilla 的 bind group，否则 `ColorModulator` 取默认值 0，文字被乘成完全透明（现象是"画了但看不见"）。

### 4.4 游戏进程不退出

`jstack` 证据：非守护线程 `"UI thread"` 停在 `ViewRootImpl.endDrawLocked` 的 `Object.wait()`，JVM 只能一直等它。三处加固：

- `mUiThread.setDaemon(true)`（即使未完全收尾也不阻塞退出）；
- `destroy()` 中 `join` 超时后 `interrupt()` 并再 `join` 一次 —— **这是唤醒 `wait()` 的正解**；
- ~~`mRenderLock.wait()` → `wait(1000)`~~ **已回退**，原因见 **4.7**。

### 4.5 界面空白：NeoForge 端 Arc3D 深度约定缺失

> **勘误（2026-09-16）**：本节原先将"tooltip 无背景"一并归因于此，并记载"Fabric 端一切正常"。
> 双端实机复测表明 **Fabric 端同样无背景**，两者并不同源 —— tooltip 背景的真正根因见 **4.6**。
> 本节的结论仅对"界面空白"成立。

现象：**打开配置/中心界面后仅画面变暗、无内容**（NeoForge）。

**根因**：NeoForge 端 `MixinRenderSystem` 构造 Arc3D 的 `ContextOptions` 时，**漏掉了 `mDepthClipNegativeOneToOne = false`**。

- 该字段类型为 `java.lang.Boolean`，Arc3D 构造函数中**显式置为 `null`**（非 Java 默认 false）；
- 值为 `null` 时 Arc3D 采用 **D3D 的 0..1 裁剪空间深度约定**，而 OpenGL 需要 **-1..1**；
- 于是 ModernUI 经 Arc3D 渲染的**离屏 UI 层**与 **tooltip 背景**全部输出空白 —— 与观察到的两个现象完全对应（而文字走的是 MC 自身的渲染路径，故仍然可见）；
- 26.1.2 原版不存在该字段（arc3d `2026.2.0` 新增），Fabric 的 26.2 移植已补上，NeoForge 端遗漏。

**修复**：在 `neoforge/.../mixin/MixinRenderSystem.java` 的 `onInitRenderer` 中补上该行，与 Fabric 端完全一致（一行，不改变原有结构）。

**过程中的错误方向与回退**：曾将界面切换通知由 `MixinMinecraft#setScreenAndShow` 改为 `MixinGui#Gui#setScreen`，实机验证无任何改善，**已回退**。
依据对比：26.1.2 原版中 Fabric 与 NeoForge 的 `open()` **完全一致**（均为 `minecraft.setScreen(...)`），保持该模式即可。

**保留的有效修复**：`GuiRenderType.PIPELINE_TOOLTIP` 补齐 bind group 声明 —— 26.2 的显式 bind-group 模型要求管线声明所用 uniform，缺失时引擎报告 `unknown and unsupported uniform` 并拒绝绑定（日志可见）。这是必要的正确性修复，但**不是本次现象的根因**；其正确形态见 **4.6**（当初"继承 vanilla GUI bind group"的做法有误，会引入多余的 sampler 槽位导致 uniform 错位）。

### 4.6 tooltip 无背景：Mixin LVT 静默失效（双端共性根因）

现象：开启"现代提示框"功能后，物品悬浮提示**只有文字、没有背景**，且 **NeoForge 与 Fabric 同时出现**。

**根因**：MC 26.2 将 `GuiRenderer.executeDrawRange` 由 **3 参数**改为 **5 参数**（追加 `int startIndex, int endIndex`），
而 `common/src/main/java/icyllis/modernui/mc/mixin/MixinGuiRenderer.java` 的 `@Inject` 处理器
仍按 26.1.2 声明 3 个目标参数 + 5 个陈旧捕获变量，参数自第 4 个起全部错位，
导致注入点 LVT 解析失败、捕获到的 `renderPass` 无效，
`renderPass.setUniform("ModernTooltip", tooltipUniforms)` **从未执行** → 管线缺少该 uniform → 背景整块不绘制。
（文字走 `MixinGuiRenderer#prepareText` 另一条路径，故仍然可见。）

| | 签名 |
|---|---|
| 26.1.2 目标 | `executeDrawRange(Supplier, RenderTarget, GpuBufferSlice)` |
| **26.2 目标** | `executeDrawRange(Supplier, RenderTarget, GpuBufferSlice, `**`int startIndex, int endIndex`**`)` |
| 修复前处理器 | `(Supplier, RenderTarget, GpuBufferSlice, GpuBufferSlice, GpuBuffer, Object, int, int, CallbackInfo, RenderPass)` |
| 修复后处理器 | `(Supplier, RenderTarget, GpuBufferSlice, int, int, CallbackInfo, RenderPass)` |

**证据**：

- 运行日志（两端各一条）：`Injection warning: LVT in ... GuiRenderer::executeDrawRange ... has incompatible changes at opcode 37`（Fabric 为 opcode 26）；
- `javap -l` 反汇编 26.2 官方包：目标方法确为 5 参数，`renderPass` 位于 slot 6 且 `Start=37`，覆盖注入点。

**修复**：处理器签名对齐为 5 个目标参数，捕获变量只保留 `renderPass`。

**验证**：

- 反汇编两端产物 class，确认新签名已落盘且一致；
- 全工程扫描 `LocalCapture` / `CAPTURE_FAILHARD` / `require = 0`，无其他静默失效点；
- 两端日志 `Injection warning` 全量检索，仅此一条。

**教训**：`CAPTURE_FAILSOFT` 的 LVT 警告**不是无害的** —— 它静默传 `null`，让功能消失而非崩溃。
原先第 5 节将其判定为"捕获失败时跳过回调，不影响运行"，该判断已被推翻。
方法论与完整踩坑记录见 [AI重要经验.md](AI重要经验.md)。

### 4.7 配置界面打开时闪烁 / 花屏

**根因**：4.4 中引入的 `mRenderLock.wait(1000)`。
`mSurface` 是**单张纹理而非双缓冲**，UI 线程必须等渲染线程消费完当前帧才能绘制下一帧（靠 `swapFrameTask` 的 `notifyAll` 同步）；
一旦超时继续绘制，就会**覆盖正在被渲染的纹理**，表现为界面打开瞬间的闪烁/花屏，过渡动画也随之错乱。

**修复**：恢复为无限等待 `mRenderLock.wait()`，退出时改由 `destroy()` 的 `interrupt()` 释放该等待。
两个目标（渲染同步 + 进程正常退出）由此同时满足。

---

## 5. 待验证 / 已知事项

| 事项 | 说明 |
|---|---|
| 4.5 的修复效果 | 界面空白已实机验证通过（NeoForge） |
| `modern_tooltip` uniform 警告 | 已消除（`PIPELINE_TOOLTIP` 改为显式声明三个 uniform 块，见 4.6） |
| Fabric 运行前置 | 必须安装 **Fabric API** 与 **Forge Config API Port**；Mod Menu 可选 |
| ~~`MixinGuiRenderer#onExecuteDrawRange`~~ | **已解决（4.6）**：原判定"捕获失败时跳过回调、不影响运行"**有误** —— 该警告正是 tooltip 无背景的根因 |
| tooltip 背景（双端） | 已实机验证通过（NeoForge + Fabric） |
| VulkanMod 集成 | 仅编译期支持，不打包、不要求安装 |
| 未收录模块 | `MixinContainerScreen`、`MixinMouseHandler`（Fabric 模块）在 NeoForge 端由 `EventHandler` + NeoForge 事件替代 |

---

## 6. 构建与发布

```bash
./gradlew :ModernUI-NeoForge:build     # neoforge/build/libs/*-universal.jar
./gradlew :ModernUI-Fabric:build       # fabric/build/libs/*-universal.jar
./gradlew build                        # 双端
```

产物发布：`*-universal.jar`。模组元数据中作者为 `BloCamLimb, Huziyang520`，贡献者 `DragonHua`，
主页与反馈地址均指向本仓库；简介为中英文，明确标注"非官方移植、请勿向原作者反馈问题"。

**协议**：LGPL-3.0-or-later（见 [LICENSE](LICENSE)）。
